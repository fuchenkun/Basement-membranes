library(fmsb) 
setwd("E:\\.生信分析资料\\BMs\\TMBradar")         #???ù???Ŀ¼
data=read.table("fmsbInput.txt",header=T,sep="\t",row.names=1,check.names=F)   #??ȡ?????ļ?
maxValue=ceiling(max(abs(data))*10)/10
data=rbind(rep(maxValue,ncol(data)),rep(-maxValue,ncol(data)),data)
#??????ɫ
colors="red"
#??????????
corStat=read.table("corStat.txt",header=T,sep="\t",row.names=1,check.names=F)
colnames(data)=paste0(colnames(data),corStat$sig)

#????????
pdf(file="radar.pdf",height=7,width=7)
radarchart( data, axistype=1 , 
    pcol=colors,                 #??????ɫ
    plwd=2 ,                     #????????
    plty=1,                      #???ߣ?ʵ??
    cglcol="grey",               #??????????ɫ
    cglty=1,                     #???????????ߣ?ʵ?? 
    caxislabels=seq(-maxValue,maxValue,maxValue/2),    #?????̶?
    cglwd=1.2,                   #??????????ϸ
    axislabcol="blue",           #?̶???ɫ
    vlcex=0.8                    #??????С
)
dev.off()


